[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
# 1. Axum 웹 프레임워크
axum = "0.7"

# 2. Tokio 비동기 런타임
tokio = { version = "1.0", features = ["full"] }

# 3. IronRDP 라이브러리 
ironrdp = { git = "https://github.com/ijake-16/IronRDP.git", branch = "master" }